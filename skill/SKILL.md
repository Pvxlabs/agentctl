# Legacy Skill location

The canonical Codex Skill is maintained at
[`skills/agentctl/SKILL.md`](../skills/agentctl/SKILL.md) and is installed with:

```bash
agentctl skill install --update
```

This file remains as a compatibility pointer for older checkouts. The
canonical Skill also covers the existing scoped API-call workflow; do not
maintain a second copy here.
